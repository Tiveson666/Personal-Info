[build]
cmd = "composer install --no-dev && npm install && npm run build"

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=${PORT:-8000}"

[env]
APP_ENV = "production"
APP_DEBUG = "false"
LOG_LEVEL = "error"
LOG_CHANNEL = "stack"
